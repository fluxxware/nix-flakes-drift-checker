# NIX-BUILD / VERSION NOTE: single source of truth for project coordinates.
# Gradle exposes these to ALL projects automatically; Java reads version at
# runtime from a generated version.properties resource; flake.nix reads the same
# file. Bump the version HERE only.
group=com.nix.flakedrift
version=1.0-SNAPSHOT